spring.application.name=smartqweb
spring.datasource.url=jdbc:mysql://localhost:3306/smrt?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===== PayHere Configuration =====

# Sandbox configuration (defaults to true)
payhere.sandbox-mode=${true}
payhere.sandbox-merchant-id=${1230795}
payhere.sandbox-merchant-secret=${NzI1MDc3MTYxMjAzMzI3MDUzMzQ4NTM3MTYwNjE0Mjc3MjY4ODc=}

# Payment URLs (constructed from base URLs)
payhere.return-url=${frontend.url}/payment/success
payhere.cancel-url=${frontend.url}/payment/cancel
payhere.notify-url=${backend.url}/api/payments/webhook

# ===== Base URLs =====
frontend.url=${https://localhost:3000}
backend.url=${https://localhost:8080}